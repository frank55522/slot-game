import { Game1SpinCommand } from '../../../sgv3/command/state/Game1SpinCommand';

export class WAY_Game1SpinCommand extends Game1SpinCommand {
    public execute(notification: puremvc.INotification): void {
        super.execute(notification);
    }
}
