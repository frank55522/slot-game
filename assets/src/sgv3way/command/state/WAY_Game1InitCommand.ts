import { Game1InitCommand } from '../../../sgv3/command/state/Game1InitCommand';

export class WAY_Game1InitCommand extends Game1InitCommand {
    public execute(notification: puremvc.INotification): void {
        super.execute(notification);
    }
}
