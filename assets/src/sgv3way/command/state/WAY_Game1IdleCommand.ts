import { Game1IdleCommand } from '../../../sgv3/command/state/Game1IdleCommand';

export class WAY_Game1IdleCommand extends Game1IdleCommand {
    public execute(notification: puremvc.INotification): void {
        super.execute(notification);
    }
}
