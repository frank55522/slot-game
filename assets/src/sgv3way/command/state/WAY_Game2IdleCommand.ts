import { Game2IdleCommand } from '../../../sgv3/command/state/Game2IdleCommand';

export class WAY_Game2IdleCommand extends Game2IdleCommand {
    public execute(notification: puremvc.INotification): void {
        super.execute(notification);
    }
}
