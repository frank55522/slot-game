import { Game2InitCommand } from '../../../sgv3/command/state/Game2InitCommand';

export class WAY_Game2InitCommand extends Game2InitCommand {
    public execute(notification: puremvc.INotification): void {
        super.execute(notification);
    }
}
