import { Game2SpinCommand } from '../../../sgv3/command/state/Game2SpinCommand';

export class WAY_Game2SpinCommand extends Game2SpinCommand {
    public execute(notification: puremvc.INotification): void {
        super.execute(notification);
    }
}
